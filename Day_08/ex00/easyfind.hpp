#include <vector>

template<typename T>
T   easyfind(T cont, int nbr)
{
    std::T<int>::iterator it = cont;

    if (it == NULL)
        throw "no container found in parameter";
    for (; *it != nbr ; it++) {
        return(*it);
    }
    throw "no similarities found";
    for(int i )
}
